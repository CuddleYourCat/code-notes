
    let editingRow = -1;
    function setColumnUpdatePostbackEvents() {
        //let $gridViewRows = $.makeArray($('.custom-report-column'));
        //$.each($gridViewRows,
        //    function(index, element) {
        //        $(element).click(function(e) {
        //            if (editingRow !== index) {
        //                editingRow = index;
        //                __doPostBack('Root$Main$ctl00$rptTemplateColumns', `Edit\$${index}`);
        //            }
        //        })

        //    });

    }